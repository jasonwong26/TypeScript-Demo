import expect from "expect";

describe("Store", () => {
  it("should handle testing", () => {
    // arrange
    const expected = 0;
    const actual = 0;

    // assert
    expect(expected).toEqual(actual);
  });
});
